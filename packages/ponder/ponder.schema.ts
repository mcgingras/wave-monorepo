import { createSchema } from "@ponder/core";

export default createSchema((p) => ({
  // --------------------------------
  // Nom -- the ERC721 base token
  // --------------------------------
  //   Nom: p.createTable({
  //   }),
}));
