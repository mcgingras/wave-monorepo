{
  "name": "proplot-monorepo",
  "version": "0.0.1",
  "description": "Prop lot protocol helps you get your Nounish ideas on-chain.",
  "scripts": {
    "build": "lerna run build --stream",
    "dev": "lerna run dev",
    "start": "lerna run start",
    "start-directory": "lerna run start --scope directory",
    "start-ponder": "lerna run start --scope ponder"
  },
  "author": "frog, adel, markus",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.2"
  }
}
